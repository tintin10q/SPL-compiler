iff (tmp : Int) {
    var tmp2 = 3;
    if (tmp2 == 3) {
            return 0;
        // It thinks we need a return here but it is not so 
    }
    return 0;
}

main () {
    var tmp = 3;
    while (tmp > 0) {
        print(tmp);
        print(" > 0\n");
        tmp = tmp -1;
    }
}