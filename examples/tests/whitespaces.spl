/* Some code in a comment

   Void int ()
   id = null;

   if(x == < 6) {}
*/

// A strange name, but parsable
CAPPS (X:Int , S:Bool) : Void
{
	// Everthing on one line
	var X = 0; var S = True; return;
}

tabbed () : Void
{
	// Indented with tabs
	return;
}
